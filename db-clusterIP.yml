apiVersion: v1
kind: Service
metadata:
  # the name should be aligned with the application.properties
  name: k8s-app-db
spec:
  ports:
  - port: 3306
    targetPort: k8s-app-db-port
    protocol: TCP
  selector:
    app: k8s-app-db
  type: ClusterIP